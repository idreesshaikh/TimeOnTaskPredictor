# λ sweep overlay — VAL SELECTION ONLY (fast subsample; see _base.yaml). Sets
# only this run's λ + output paths; compare with scripts/select_lambda.py, copy
# the winner into configs/vlm.yaml, then run the full-data finals. TEST untouched.
base: _base.yaml

paths:
  output_dir: artifacts/sweeps/vlm_lam25
  train_card: artifacts/sweeps/vlm_lam25_card.md

lupi:
  lambda: 0.25

train:
  run_name: qwen3vl4b-sweep-lam25
