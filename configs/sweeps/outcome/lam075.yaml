# λ=0.75, OUTCOME-ONLY teacher grid — see outcome/lam025.yaml for the full
# rationale. Identical to ../lam075.yaml except the teacher parquet + paths.
base: ../lam075.yaml

paths:
  output_dir: artifacts/sweeps_outcome/vlm_lam075
  train_card: artifacts/sweeps_outcome/vlm_lam075_card.md

lupi:
  teacher_preds: artifacts/lupi_teacher_preds_outcome.parquet

train:
  run_name: qwen3vl4b-v3-lam075-outcome
