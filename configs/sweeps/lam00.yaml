# λ sweep overlay — VAL SELECTION ONLY (fast subsample; see _base.yaml).
# λ=0 is the ablation reference: plain SFT on the true label, no teacher —
# "does distilling the privileged features help at all?". Sets only this run's
# λ + output paths; compare with scripts/select_lambda.py, copy the winner into
# configs/vlm.yaml, then run the full-data final conditions. TEST never touched.
base: _base.yaml

paths:
  output_dir: artifacts/sweeps/vlm_lam00
  train_card: artifacts/sweeps/vlm_lam00_card.md

lupi:
  lambda: 0.0

train:
  run_name: qwen3vl4b-sweep-lam00
